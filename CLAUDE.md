# مهووس — دليل التطوير (CLAUDE.md)

أداة تسعير ذكية لمتجر عطور/تجميل سعودي على **سلة**. Python/Streamlit.
المعمارية: موجّه رفيع `app.py` + حقن اعتماديات (Container) + `ui/` (صفحات) + `services/` + `engines/` + `utils/` + `api/`.
**المتجر حيّ** — أي تعديل خاطئ يوقف منتجات أو يفسد بيانات. اعمل بحذر جرّاح.

## 🚦 بوّابة الاختبارات (إلزامية — قبل وبعد أي تغيير)
```
.venv/Scripts/python.exe -m pytest -q --ignore=tests/test_api.py
```
الأساس الأخضر المعروف على **main** (الفرع الرئيسي الوحيد منذ 2026-07-16؛ `master` تقاعد): **1315 passed, 0 failed** (2026-07-16). عدد الصفحات في `PAGES`: **17**. نقص العدد أو أي فشل ⇒ **تراجَع فوراً، لا تُكمل**.
- `tests/test_api.py` يُستبعد لأن `fastapi` غير مثبّت — فجوة بيئة لا خطأ كود.
- `ruff` غير مثبّت ⇒ للتحقّق النحوي استخدم `python -m py_compile <file>`.
- خطّاف **git pre-commit** يفرض هذه البوّابة تلقائياً ويمنع أي commit فاشل.

## 🔒 قواعد الاشتباك (حديدية)
1. **جراحيّ:** أي تغيير أكبر من ~30 سطراً أو يحذف ميزة ⇒ **توقّف واستأذن** قبل التنفيذ.
2. **grep قبل أي حذف:** ابحث عن الرمز في كامل المستودع شاملاً `tests/` و`getattr/importlib/__all__/api`.
   مستخدَم في اختبار ⇒ حدّث الاختبار في نفس الـcommit أو أبقِه مع `# TEST-ONLY`. **احذف بالاسم لا برقم السطر.**
3. **لا تلمس `#PRESERVED_LOGIC`** (≈90 موضعاً) ولا سلوك `normalize` / التسعير / المطابقة / التصدير.
4. **Git:** فرع جديد + commit صغير لكل خطوة + وسم الحالة الخضراء قبل البدء (مسار تراجع).
5. **الأسرار:** من `.env` / `.streamlit/secrets.toml` فقط (`GEMINI_API_KEY`، `OPENROUTER_API_KEY`). لا تكتب مفتاحاً في الكود أبداً.
6. **قاعدتا بيانات (مصحَّحة 2026-07-22 بتحقّق كود مباشر):** `pricing_v18.db` هي القاعدة الموحّدة الحيّة الفعلية — تضم كتالوجنا **و** بيانات المنافسين **و** كل الجداول الإجرائية معاً بملف واحد (`utils/db_manager.py:_DB_NAME` يكتب إليها فعلياً: `our_catalog`، `send_log`، `decisions`، وأيضاً `competitor_products_store`). `perfume_pricing.db` طبقة "v4" تجريبية صغيرة شبه غير مستخدمة (`ai_decisions_log`/`price_anomalies`/`notifications` عبر `conf/settings.py:_resolve_db_path`) — **ليست** "ذكاء المنافسين" كما ورد سابقاً هنا خطأً. تأكّد من الملف الصحيح قبل أي استعلام.
7. **الرسائل عربية** ومفهومة لصاحب متجر غير تقني؛ لا تكسر صفحة عند غياب البيانات (حالات فارغة واضحة).
8. **سجلّ القرارات إلحاقيّ حصراً** عبر `append_price_decision` (INSERT صرف بطابع زمني بدقّة الثانية) — **يُمنع** `upsert_price_history` للقرارات (سلوكها UPSERT يوميّ يستبدل فيمحو رحلة القرار).
9. **القراءات التحليلية `mode=ro`** (الكتابة تفشل على مستوى SQLite)؛ و**معاينة `streamlit.testing.v1.AppTest` داخل العملية قبل دمج أي واجهة** (لا بوّابة دخول — أُزيلت بقرار المالك 2026-07-09؛ دمج مشروط بنجاح معايير صريحة).
10. **كاتبٌ واحد للمستودع؛ العمل الموازي (سقالات مهارات ونحوها) خارج مجلد المشروع.** ملفات/تعديلات غريبة في الشجرة ⇒ **توقّف وجرد** — لا حذف ولا commit أعمى ولا checkout يتجاوزها (استعمل `git branch -f` للدمج FF عند اعتراض تعديلات غير مُودَعة).
11. **`services/pricing_service.py` تجريبي غير موصول** — دماغ التسعير الحيّ هو `services/opportunity_service` (سياسة v1)؛ **لا يُوصَل** قبل مواءمة v1 (مرجع الأقل المتوفر · حاجز ±20% من وسيط السوق · أرضية التكلفة وحدها · إلغاء الذروة الزمنية/تعديل المخزون/تعظيم الإيراد · خلف موافقة المالك).

## 🧭 المهارات المساعدة (في ~/.claude/skills)
- `tactical-harden` — تحصين شامل قبل الإطلاق عبر 6 مراحل مع إذن بينها.
- `mahwous-salla-pipeline` — تحقّق من كود المبرمج + بناء/إصلاح سيناريو Make + تشخيص التصنيف/الوصف الخاطئ.
- `/code-review` قبل كل دمج · `verify` لتشغيل التطبيق فعلياً · `/security-review` للأمن.

## 🔍 أنت طبقة أمان مستقلة
ادّعاءات المبرمج **بيانات لا حقائق**. تحقّق بـgrep/read من الكود الفعلي قبل تصديق أي «تمّ الإصلاح / كود ميت / مكرّر».
احذر الإيجابيات الكاذبة: كود ميت يستخدمه اختبار، «مكرّرات» مختلفة السلوك، قيم احتياطية مثبّتة، ميزات محكومة بمفتاح. التفاصيل في مهارة `mahwous-salla-pipeline`.
