# خطة العرض الشاملة — المنافسون · نتائج الأقسام · بطاقات المنتجات

**الحالة (2026-07-25): البند 1 والبند 2 مُنفَّذان ومدموجان · البند 4 قيس · البند 3
وحده خلف بوابة المالك.** الأساس بعد التنفيذ: **1,430 أخضر** على `main`.
كل بند مسنود بـ`ملف:سطر` مُتحقَّق. البنود التي تتجاوز ~30 سطراً أو تحذف ميزة **خلف
بوابة المالك**.

---

## 0. ما يعمل جيداً — لا يُلمس

تحقّقتُ منه بالكود قبل اقتراح أي «تحسين»، منعاً لإصلاح ما ليس مكسوراً:

| القدرة | الدليل |
|---|---|
| ترقيم صفحات الأقسام | `ui/pages/_section_page.py:310` (`paginate`) و`:316` |
| تبديل بطاقات/جدول | `_section_page.py:163` |
| فلاتر (سعر · نسبة تطابق · عدد منافسين · توفّر) | `ui/components/filter_bar.py:136,148,156,112` |
| حقن CSS للبطاقة **مرة واحدة لكل جلسة** لا لكل بطاقة | `comparison_card.py:646` (محروس بـ`session_state`) |
| ترقيم في المفقودات/الجديد/المعالجة | `missing.py:1103` · `new_at_competitors.py:426` · `processed.py:225` |

**تصحيح لخطة التوسّع:** بند «ترقيم الواجهة» فيها **منجَز أصلاً** في الأقسام
الرئيسية. الفجوة الحقيقية للترقيم في **لوحة المتاجر المكتشفة** (§1).

---

## 1. المحور الأول: عرض المنافسين

**الموضع:** `ui/pages/scraper.py:164-208` (لوحة «متاجر عطور جديدة في السوق»).

### الفجوات المُتحقَّقة

| # | الفجوة | الدليل | المخاطرة |
|---|---|---|---|
| 1‑أ | حلقة عرض **بلا حدّ ولا ترقيم**: كل متجر مكتشف يُرسم بصفّه وزرّه | `scraper.py:190` (`for d in found`) | منخفضة |
| 1‑ب | لا عتبة دنيا لعدد المنتجات: متجر بمنتجَين يظهر بنفس بروز متجر بـ5,000 | لا فلتر في `:185-190` | منخفضة |
| 1‑ج | لا إضافة جماعية — نقرة لكل متجر (30 متجراً = 30 نقرة) | `:200` زرّ لكل صفّ | منخفضة |
| 1‑د | بعد الإضافة لا يتغيّر شكل الصفّ حتى إعادة التشغيل | `:200-208` بلا تحديث حالة | منخفضة |
| 1‑هـ | الإضافة **لا تكشط** المتجر — يبقى بلا بيانات حتى الجولة التالية | `:202` `add_competitor` فقط | متوسطة (توقّع مضلِّل) |

### ✅ مُنفَّذ بالكامل (2026-07-25، بإذن المالك)

| الفجوة | الكوميت | كيف |
|---|---|---|
| 1‑أ ترقيم | `7810d53` | `paginate`/`render_pagination` القائمتان، 10/صفحة |
| 1‑ب عتبة | `7810d53` | حدّ أدنى للمنتجات + بحث بالاسم **أو المعرّف** |
| 1‑ج إضافة جماعية | `7810d53` | مربّعات تحديد + «أضِف المحدَّد» |
| 1‑د وسم «مُضاف ✓» | `7810d53` | من قائمة المنافسين الحالية (يتحدّث تلقائياً بعد الإضافة) |
| 1‑هـ «الإضافة لا تكشط» | `321a4be` | تنبيه يذكر موعد الجولة الليلية (٣:٣٠ متحقَّق) والمخرج اليدوي |

المنطق خالص في `filter_discovered` (نمط `kpi_breakdown`) — 20 اختباراً للتصفية
والوسم وصمود الجماعي. **الحجم الفعلي 87 سطراً** لا 40-60 كما قُدِّر هنا.
معاينة AppTest بـ25 متجراً مُحاكى (جدول `discovered_stores` فارغ فعلياً = 0 صفّ،
فبلا محاكاة لا يُنفَّذ المسار الجديد أصلاً). الأساس بعده: **1,430 أخضر**.

**✅ أُغلق المتبقّي (2026-07-25 مساءً، `38a0e55`):** مربّع «🔄 اكشطه فور الإضافة»
داخل اللوحة نفسها — يعمل للإضافة المفردة وللجماعية (بشريط تقدّم). مطفأ افتراضياً
لأن كشط متجر ~12 ثانية. وحارسان ضد النجاح الكاذب: `saved=0` لا يُعلَن نجاحاً،
وفشل الكشط تحذيرٌ لا خطأ إضافة (المتجر بقي مُضافاً وتلتقطه الجولة الليلية).

---

## 2. المحور الثاني: عرض النتائج في الأقسام

**الموضع:** `ui/pages/_section_page.py` (318 سطراً) — البنية سليمة ومرقّمة.

### ✅ قيس (2026-07-25) — والنتيجة: لا تغيير مطلوب هنا

كل صفحات الأقسام الستّ تمرّ العتبة بفارق واسع (19–24م.ث دافئ). **التفصيل في §5.**
الأداة: `scripts/measure_page_open_times.py` مقابل عتبة **150 م.ث** المعتمدة في
`docs/خطة-التطوير-الشاملة.md`.

### فرضيات تحتاج إثباتاً (لا تُنفَّذ قبله)
- هل حجم الصفحة (عدد البطاقات) مناسب أم يحتاج ضبطاً؟
- هل يحتاج المالك فرزاً صريحاً داخل القسم (الأكبر فجوةً / الأحدث)؟ بعض الأقسام
  فيها ترتيب مسبق («الأهم أولاً») — يلزم جرد أيّها يملكه وأيّها لا.

**الخلاصة:** هذا المحور **مؤجَّل حتى قياس نظيف** — اقتراح تحسين بلا قياس هنا مخاطرة
بإفساد صفحات تعمل.

---

## 3. المحور الثالث: بطاقات المنتجات

**المواضع:** `ui/components/comparison_card.py` (43 ك.ب — بطاقة الحَلَبة، مدخلها
`render_row_arena:692`) · `ui/components/product_card.py` (197 سطراً).

### مكوّنان غير موصولين — والجرد الشامل صحّح وصفي الأول

**تصحيح (2026-07-25):** كتبتُ هنا «ميتان تماماً · صفر استخدام»، وكان ذلك مسنوداً
بـ`grep` على `ui/` و`app.py` **فقط**. الجرد الشامل (شاملاً `tests/` و`api/`) يقول غير ذلك:

| المكوّن | الحجم | موصول بصفحة؟ | اختبارات | الحكم الصحيح |
|---|---|---|---|---|
| `expert_card.py` | 983 سطراً | لا | **16 اختباراً** يشغّلون `analyze` و`audit_competitors` و`build_expert_card_html` | **ميزة مكتملة ومُختبَرة لم تُوصَل** — لا كود ميت |
| `product_timeline.py` | — | لا | — | **متقاعد فعلياً** إلى المحجر خارج الشجرة (`61d428b`) |

**إيجابية كاذبة مُلتقَطة:** `api/main.py:180` فيه دالة اسمها `product_timeline` — نقطة
نهاية FastAPI تعيد `entity.timeline`، **لا علاقة لها بالمكوّن**. من يبحث بالاسم يستنتج
خطأً أنه مستخدَم. و`.claude/settings.local.json` يذكره في سلسلة صلاحية `py_compile` فقط.

### ⛔ خطر يحسم خيار «الإحياء»: تسعير موازٍ ثالث

`expert_card._strategist_price` **سياسة تسعير كاملة خاصّة به** («بذرة Batch 2»)، تختلف
جوهرياً عن v1 الحيّة في `services/opportunity_service`:

| | v1 الحيّة | `_strategist_price` |
|---|---|---|
| المرجع | الأقل المتوفر | أرخص منافس **موثوق** |
| الحاجز | **±20% من وسيط السوق** (`BAND_PCT`، يمنع الشذوذ) | **لا حاجز وسيط إطلاقاً** |
| الكسر | لا كسر | 1%–4% متناسب مع «سلطة» المتجر |
| الأرضية | التكلفة (خامدة: `cost=0`) | هامش ربح |

فإحياؤه كما هو يعرض على المالك أسعاراً قد تخرج عن حاجز الأمان الذي تفرضه السياسة
الحيّة — وهو نفس السبب الذي يمنع وصل `services/pricing_service.py` (قاعدة 11 في
`CLAUDE.md`). **الإحياء يلزمه مواءمة مع v1 أولاً، لا توصيل خلف تبديل.**

**الخيارات بعد الجرد — القرار للمالك:**
1. **إبقاء `expert_card` كما هي** (موصى به): ميزة مُختبَرة قد تُحيا لاحقاً بعد مواءمة
   تسعيرية. تقاعدها يعني حذف 16 اختباراً وإسقاط الأساس بمقدارها.
2. **اكتمل تقاعد `product_timeline`** (`61d428b`): نُقل إلى محجر **خارج الشجرة** ولم
   يُحذف، وفق قرار المالك.
3. **إحياء `expert_card`:** خلف مواءمة سياسة v1 + بوابة مالك.

⚠️ **الحذف بالاسم لا برقم السطر**، والقاعدة الحديدية في `CLAUDE.md`: مستخدَم في اختبار
⇒ حدّث الاختبار في نفس الـcommit أو أبقِه مع `# TEST-ONLY`.

---

## 4. الترتيب المقترح (عائد ÷ مخاطرة)

| # | البند | المخاطرة | الحالة |
|---|---|---|---|
| 1 | لوحة المتاجر المكتشفة (§1) — ترقيم + عتبة + إضافة جماعية | منخفضة | ✅ `7810d53` |
| 2 | توضيح «الإضافة لا تكشط» (§1‑هـ) — سطر واحد | منخفضة جداً | ✅ `321a4be` |
| 3 | قرار المكوّنَين الميتين (§3) | متوسطة | ✅ تقاعد `product_timeline`؛ `expert_card` محفوظ |
| 4 | قياس زمن الأقسام ثم قرار (§2) | — | ✅ قيس (انظر §5) |

**لا يبدأ أي بند بلا موافقة صريحة، ولا يُدمج بلا بوابة اختبارات خضراء.**

---

## 5. نتيجة القياس (2026-07-25)

13 من 14 صفحة ظاهرة تمرّ عتبة 150م.ث، والقياس تحت ضغط ذاكرة (0.36 غ.ب حرّة من
7.78) فهو **حدّ أعلى**. التجاوز الوحيد 🕷️ كشط المنافسين **219م.ث**.

**⛔ لا تُضِف فهرساً لـ`scraped_stats`:** `EXPLAIN QUERY PLAN` يقول
`SCAN … USING COVERING INDEX idx_cps_competitor_updated` — الاستعلام مُحسَّن أصلاً،
والـ80م.ث كلفة تجميع 435 ألف مدخل. الحلّ الوحيد جدول ملخّص يُحدَّث وقت الكشط
(نمط `opportunity_scores`) ⇒ >30 سطراً ويمسّ مسار الكشط ⇒ بوابة مالك.

**والأهمّ: المقياس نفسه يُخفي ما يشكو منه المالك.** وسيط 3 محاولات يقيس الفتح
**الدافئ**؛ الفتح **البارد** أثقل 5–54×: «جديد عند المنافسين» 7,360م.ث ثم 136م.ث ·
لوحة التحكم 1,546 ثم 31 · الرادار 1,045 ثم 134 · الكشط 1,040 ثم 219. مصدره قراءة
القاعدة/الفهارس من القرص أول مرّة، و**كاش الجلسة لا يُصلحه**. المسار المقترح:
تسخين مسبق عند تشغيل الخادم (لا يمسّ منطقاً) ⇒ بوابة مالك.
التفصيل في مذكّرة `page-open-timings-20260725`.

---

## 6. أرقام مرجعية (2026-07-25)

- `PAGES` = 17 (14 ظاهرة + 3 مخفيّة) — **كلها تفتح بلا خطأ** (فحص AppTest لكل صفحة).
- مجلد `ui/components/` = 4,619 سطراً في 15 ملفاً.
- أساس الاختبارات: **1,430 أخضر** على `main` (وسم `green-1430`). المسار خلال اليوم:
  1,390 ← 1,406 (`1bb7a46`، +**16** لا 15 لأن `parametrize` بثلاثة مفاتيح يُعَدّ
  ثلاثة) ← 1,410 (`321a4be`) ← 1,430 (`7810d53`).
- أقسام آخر تحليل (`run_1784978382`، 2026-07-25، 110.7 دقيقة): رفع 4,869 · خفض 878 ·
  معتمد 1,740 · مراجعة 1,192 · مستبعد 2,839 · مفقود 25,979. المجموع 11,518 =
  الكتالوج بالضبط (`gap=0` · `duplicates=0` · `balanced=true`)، ومُنقَذ حتمياً **225**.
- الأرقام السابقة (رفع 4,766 · خفض 905 · معتمد 1,760 · مراجعة 976 · مستبعد 3,111) من
  تحليل 2026-07-19 — تُقرأ كمقارنة لا كحالة راهنة.
