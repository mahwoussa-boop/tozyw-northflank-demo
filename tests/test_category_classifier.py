"""tests/test_category_classifier.py — تصنيف المنتج لشجرة متجر مهووس الحقيقية.

يثبت إصلاح الخلل: لم يعد كل منتج «عطور رجالية» — العناية/الجسم/المكياج تأخذ
تصنيفها الصحيح، والعطور تُصنَّف حسب النوع/الجنس.
"""
import pytest

from services.category_classifier import classify_category


# ── الاختبارات القاطعة: المنتجات التي ظهرت خاطئة عند المستخدم ──
def test_body_butter_is_body_moisturizer() -> None:
    assert classify_category("زبدة لترطيب الجسم باشن فروت من سوما - 350 مل") == "مرطبات الجسم"


def test_schwarzkopf_hot_oil_is_care_not_perfume() -> None:
    # لا يحوي الاسم «شعر»، لكن «علاج+زيت» + ماركة Schwarzkopf ⇒ عناية لا عطر
    name = "بوناويل علاج الزيت الساخن بالقمح من شوارزكوف 225مل"
    assert classify_category(name) == "العناية"                      # بالاسم وحده (علاج+زيت)
    assert classify_category(name, brand="Schwarzkopf") == "العناية"  # بإشارة الماركة


def test_care_brand_signal_overrides_perfume_default() -> None:
    # اسم غامض + ماركة عناية ⇒ عناية (لا عطور رجالية الافتراضية)
    assert classify_category("منتج بلا كلمات دالّة", brand="Pantene") == "العناية"


@pytest.mark.parametrize("name,expected", [
    # عناية وجسم ومكياج
    ("لوشن ترطيب الجسم بالشيا 250مل", "مرطبات الجسم"),
    ("بودرة الجسم المعطرة", "بودرة الجسم"),
    ("أحمر شفاه مات من ماك", "المكياج"),
    ("ماسكرا لتطويل الرموش", "المكياج"),
    ("شامبو للشعر بالكيراتين", "العناية"),          # عناية شعر ⇒ العناية
    ("بلسم مرطّب للشعر التالف", "العناية"),
    ("حمام كريم بزيت الأرغان", "العناية"),
    ("غسول وجه منظّف عميق", "العناية"),
    ("مقشر سكر للجسم", "العناية"),
    ("مست شعر برائحة منعشة", "عطور الشعر"),         # عطر شعر ⇒ عطور الشعر
    ("بخور فاخر كمبودي", "العود و البخور"),
    ("شمعة معطرة برائحة الفانيلا", "شموع معطرة"),
    ("معطر جو للمنزل لافندر", "معطرات هواء"),
    ("بادي مست فيكتوريا سيكريت", "عطور الجسم"),
])
def test_non_perfume_categories(name, expected) -> None:
    assert classify_category(name) == expected


@pytest.mark.parametrize("name,gender,expected", [
    ("عطر سوفاج للرجال او دو بارفيوم 100مل", "", "عطور رجالية"),
    ("عطر نسائي جذّاب", "", "عطور نسائية"),
    ("عطر ديور", "نسائي", "عطور نسائية"),
    ("عطر نيش زيرجوف للرجال", "", "عطور النيش رجالية"),
    ("تستر عطر شانيل نسائي", "", "عطور التستر نسائية"),
    ("عطر أطفال لطيف", "", "عطور الأطفال"),
    ("بديل عطر فاخر رجالي", "", "بدائل العطور رجالية"),
    ("عطر فرموني للجنسين", "", "عطور فرمونية للجنسين"),
])
def test_perfume_categories(name, gender, expected) -> None:
    assert classify_category(name, gender) == expected


def test_unknown_gender_perfume_goes_to_general_perfumes() -> None:
    # سياسة المالك: عطر مجهول الجنس ⇒ «العطور» العامّ (لا فرض «رجالية» خطأً)
    assert classify_category("عطر غامض بلا إشارات") == "العطور"
    # لكن جنس صريح في الاسم ⇒ مُجنَّس صحيح
    assert classify_category("عطر فخم للرجال") == "عطور رجالية"
    assert classify_category("عطر فخم للنساء") == "عطور نسائية"


# أسماء تصنيفات متجر مهووس الورقية الفعلية (من store_categories — السايت ماب/سلة).
# العقد: كل ما يُخرجه المُصنِّف يجب أن يكون منها، وإلا فشل الإنشاء بـ422 (categories=[]).
_REAL_STORE_CATEGORIES = frozenset({
    "العطور",
    "العناية", "المكياج", "مرطبات الجسم", "بودرة الجسم", "عطور الشعر",
    "العود و البخور", "عود طبيعي", "شموع معطرة", "معطرات هواء", "عطور الجسم",
    "إكسسوارات العطور", "عطور الأطفال", "عطور التستر نسائية", "عطور التستر رجالية",
    "عطور التستر جديدة", "عطور النيش نسائية", "عطور النيش رجالية",
    "عطور النيش للجنسين", "عطور فرمونية نسائية", "عطور فرمونية رجالية",
    "عطور فرمونية للجنسين", "بدائل العطور نسائية", "بدائل العطور رجالية",
    "بدائل العطور جديدة", "عطور نسائية", "عطور رجالية",
})


@pytest.mark.parametrize("name,brand", [
    ("زبدة ترطيب الجسم", ""), ("أحمر شفاه ماك", ""), ("شامبو كيراتين", ""),
    ("بخور كمبودي فاخر", ""), ("عود طبيعي ماليزي", ""), ("شمعة معطرة", ""),
    ("معطر جو منزل", ""), ("بادي مست", ""), ("إكسسوار عطر", ""),
    ("عطر أطفال", ""), ("تستر شانيل رجالي", ""), ("تستر شانيل نسائي", ""),
    ("عطر نيش للرجال", ""), ("عطر فرموني", ""), ("بديل عطر نسائي", ""),
    ("عطر رجالي", ""), ("عطر نسائي", ""), ("عطر كلاسيكي فخم", ""),
    ("منتج غامض", "Schwarzkopf"),
    ("سيروم وجه", ""), ("بودرة الجسم", ""), ("علاج زيت ساخن", "Pantene"),
])
def test_every_output_is_a_real_store_category(name, brand) -> None:
    out = classify_category(name, brand=brand)
    assert out in _REAL_STORE_CATEGORIES, f"تصنيف غير موجود في المتجر: {out!r} ⇒ 422"


@pytest.mark.parametrize("name,brand,expected", [
    # مستحضرات كانت تسقط خطأً إلى «العطور» (بي بي/سي سي كريم، كريم أساس، ماركة نوت)
    ("بي بي كريم مضاد للعيوب من نوت - 04 ميد", "", "المكياج"),
    ("دييجو دالا بالما كريم أساس جيشا", "", "المكياج"),
    ("عطر تجريبي", "نوت", "المكياج"),
    ("عطر تجريبي", "NOTE", "المكياج"),
    # حرّاس الثبات: عطور حقيقية تبقى عطوراً (فحص الماركة على حقل brand حصراً)
    ("عطر بنتلي ابسولو او دو بارفيوم 100مل", "", "العطور"),
    ("ديور سوفاج 100مل", "", "العطور"),
    ("عطر بلاك نوت", "", "العطور"),   # «نوت» في الاسم لا الماركة ⇒ يبقى عطراً
])
def test_cosmetics_bb_cream_and_note_brand(name, brand, expected) -> None:
    assert classify_category(name, brand=brand) == expected
