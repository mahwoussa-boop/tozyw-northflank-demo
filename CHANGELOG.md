# سجل التغييرات — مهووس للتسعير الذكي

## [غير مُصدَر] — 2026-06-27

### 🟢 ملاحظات المالك: فلتر التوفّر + تنظيف الصور + إشعارات حقيقية + سقف أعلى

- **فلتر «🟢 التوفّر»** أُضيف لشريط فلاتر كل الأقسام (الكل/🟢 متوفر/🔴 نفذت) عبر
  `filter_by_availability` في [filter_bar.py](ui/components/filter_bar.py) — vectorized
  على عمود `رابط_المنافس` مقابل مجموعة الروابط النافذة، موصول في
  [_section_page.py](ui/pages/_section_page.py). فالنفاذ/التوفّر صار **شارة داخل
  البطاقات + فلتر** في كل التصنيفات (لا صفحة منفصلة فقط).
- **تنظيف الصور**: أُزيل الحدّ الملوّن حول صور بطاقات «جديد/نفذت» (حدّ محايد) —
  الحالة تبقى بالشارة، صورة أنظف.
- **الإشعارات لم تعد نصاً عالقاً**: شارة «🔔 إشعار جديد» صارت موسّعاً يسرد الإشعارات
  الحقيقية مع زرّ «✓ تحديد الكل كمقروء» ([app.py](app.py)) — كانت تُكتب بلا واجهة قراءة.
- **سقف العرض**: رُفع حدّ «جديد/نفذت» من 500 إلى 10,000 (يعكس الأرقام الحقيقية).
- اختبار جديد لفلتر التوفّر. **698 اختبار ناجح.**

### ✅ تدقيق شامل: كل نص ظاهر + كل خدمة (أعلى مستوى)

- **تدقيق النصوص**: فُحصت 101 سلسلة واجهة ظاهرة للمستخدم — كلها عربية صحيحة بلا
  أخطاء/نصوص مؤقتة/تسرّب إنجليزي. وُحّد مصطلح «الذكاء الصناعي» → «الذكاء الاصطناعي»
  (7 مواضع في review_panel.py / review_ai.py / ai_engine.py) لاتساق احترافي.
- **فحص الصحة الشامل (e2e_health_check)**: 6/6 PASS — اتصال الذكاء، تشغيل مصغّر
  30/30 (0% فقدان)، الدائرة المقفلة 143,841 متوازنة (0% تسرّب)، سلامة الروابط،
  سلامة أزرار الإرسال.
- **التحقق من الخدمات**: التحليل (gap=0)، المطابقة (rapidfuzz)، التصنيف، التصدير،
  الكشط (24 متجراً)، الذكاء (AI Router)، التعلّم/الشذوذ/المعايرة/اللقطات — كلها تعمل.
- **697 اختبار ناجح.**

### 🟢 لوحة التحكم: مدّة آخر تحليل + صفحة الكشط: تفاصيل الكشطة وتمييز بلا معرّف

- **مدّة التحليل**: حقل `duration_sec` في `AnalysisResult` يُلتقط في
  [app.py](app.py) `_make_analyze` ويُعرض في ملخّص لوحة التحكم («⏱️ استغرق …»).
  آمن للّقطات القديمة (getattr افتراضي). يظهر بعد أول تحليل جديد.
- **صفحة الكشط**: بعد كل كشط تظهر «🆕 X جديد · 🔄 Y محدّث · 🔴 Z نفذت» (عبر
  `ScraperService.last_scrape_run`)، والمنافسون **بلا معرّف متجر** يُميَّزون بشارة
  كهرمانية واضحة. اختبار جديد في [test_scraper_catalog.py](tests/test_scraper_catalog.py).

### ✅ تحقّق: أنظمة التعلّم/الشذوذ/اللقطات تعمل فعلياً

تحقّق headless مباشر (قاعدة مؤقتة): كشف الشذوذ يعلّم 0 ر.س (`data_error`) و99999
(`sudden_spike`)؛ معايرة الثقة تنخفض 0.90→0.225 بعد 75% رفض؛ لقطات السوق تُحفظ مع
الموقع السعري؛ ذاكرة الأنماط وإحصاءات التقييم تعمل. **الأداء**: كل حلقات العرض على
بيانات مُرقَّمة/محدودة — لا O(n²) في مسار العرض.

### 🟢 إثراء «جديد/نفذت»: كشط كل المنافسين المُعرَّفين (24 متجراً)

كُشطت كل المتاجر ذات `mahally_store_id` لتفعيل «جديد/نفذت» على نطاق واسع وإضاءة
شارات «نفذت» داخل بطاقات الأقسام (مثال: سولارا 29,459 منتج · 1,292 جديد · 1,235 نفذت).

### 🟢 بطاقة المنتج: شارة توفّر المنافس (🔴 نفذت / 🟢 متوفر) في كل الأقسام

كل منافس داخل بطاقة المقارنة (الحَلَبة + باقي المنافسين + بطاقة المفقودات) يعرض الآن
شارة توفّر. المصدر: مجموعة روابط المنتجات النافذة (`availability=0`) من قاعدة المنافسين،
عبر `_oos_links()` في [comparison_card.py](ui/components/comparison_card.py) — مخزّنة
مؤقتاً على مستوى العملية بمفتاح `getmtime` للقاعدة (استدعاء O(1) بعد أول بناء، وتحديث
تلقائي بعد كل كشطة). تُحقن عبر `_enrich_availability` في `row_to_arena` فتغطّي الأقسام
الخمسة دفعةً واحدة. الشارة تظهر فقط حين يكون التوفّر معروفاً (لا تخمين). تحقّق حيّ على
البيانات المحفوظة: سعر أعلى 317 شارة · أقل 185 · موافق 190 · مراجعة 137. اختباران جديدان.

### 🟢 جديد/نفذت: كشط حقيقي + نسبة المنتج الجديد لمنافسه

كُشط Jadastore فعلياً (1,347 منتج، 8.8s): سُجّلت كشطة في `scrape_runs`، أُضيف عمود
`availability`، **11 جديد** (منسوبة كلها لـ Jadastore) + **8 نفذت**. تحقّق: الفلتر
بالمنافس والفترة (3/7/14/30 يوم) يعمل، والمنتجات الجديدة تظهر في «🆕 جديد عند المنافسين»
منسوبةً للمنافس الذي أضافها.

### 🟢 لوحة التحكم: «🚀 أكبر فرص الربح» (أهم 5 منتجات أرخص من المنافس)

أُضيفت بطاقة أعلى 5 منتجات في قسم «🟢 سعر أقل» (سعرنا < المنافس) مرتّبة تنازلياً
بأكبر فجوة (المنافس − سعرنا) = أكبر ربح قابل للاسترداد برفع السعر، عبر دالة خالصة
`top_urgent_products` في [dashboard.py](ui/pages/dashboard.py) (حساب مُتجَّه على
القسم كاملاً ثم اقتطاع 5 — بلا حلقات عرض ثقيلة، ترميز HTML آمن، يستبعد ما المنافس
فيه أرخص). **ملاحظة دلالية مهمة:** «🔴 سعر أعلى» = سعرنا أعلى (خطر)، «🟢 سعر أقل»
= سعرنا أقل (فرصة رفع) — البطاقة تستهدف الثانية. اختبار جديد في
[test_pages_logic.py](tests/test_pages_logic.py). **693 اختبار ناجح.**

### ✅ تحقّق: تحليل كامل بلا كاش — حفظ بيانات مثالي (gap=0)

شُغّل تحليل كامل **بلا أي كاش** (`use_cache=False`, AI مفعّل) على كتالوج 11,437 منتج.
النتيجة: التوزيع = 11,437 = مجموع الأقسام بالضبط · **gap=0 · تكرار=0** · الدائرة
المقفلة متوازنة (129,647 = 20,378+6,495+94,224+8,550). الزمن ~40 دقيقة (العنق:
مطابقة المحرّك CPU-bound، مرّة واحدة ثم تُكاش). الأرقام موثّقة في ذاكرة المشروع.

### 🟢 تفعيل: مفتاح OpenRouter جديد + حارس تكلفة لـ route_review

**المفتاح:** استُبدل المفتاح المُستنفَد بآخر فعّال (رصيد $4.11) في `.env` و
`.streamlit/secrets.toml` (كلاهما مُتجاهَل من git — لا يُرفع أبداً). تحقّق حيّ:
نداء واحد → 200 OK، الرد «OK»، التكلفة **$0.000004**. AI Router على 16 منتج مراجعة:
**16 داخل/16 خارج، صفر أخطاء، 14 حُسِمت تلقائياً** (2 تطابق · 12 مفقود) — الطبقة حيّة.

**حارس التكلفة (الأهم لرصيدك):** `route_review` كان **بلا أي سقف** — يعالج قسم
المراجعة كاملاً مهما كبر = تكلفة غير محدودة (بينما `route_excluded` فيه `max_items=500`).
أُضيف `max_items=300` افتراضياً في [ai_router_service.py](services/ai_router_service.py):
الفائض يبقى في المراجعة (قانون عدم الفقدان محفوظ — تحقّق: 500→500) مع ملاحظة شفافة.
السلسلة مرتّبة أصلاً بالأرخص أولاً (`gemini-2.5-flash` $0.15/M). **تكلفة التحليل الكامل
المتوقّعة ~$0.02-0.04** (≈ 100+ تحليل ضمن رصيدك).

### 🔴 إصلاح: طبقة الذكاء تفشل بصمت عند نفاد مفتاح OpenRouter (403)

**التشخيص (تدقيق عميق على البيانات الحيّة، 3 طرق):** كل نداءات AI Router تفشل
(«30 داخل/30 خارج» لكن 0 حسم — الكل يبقى في المراجعة). تتبّع السبب عبر 3 طبقات
تبتلع الخطأ (`ai_router` → `AIService.call` → `_call_openrouter`) كشف **HTTP 403
«Key limit exceeded»**: مفتاح OpenRouter استنفد حدّه. والأسوأ: `_call_openrouter`
**لا يعالج 403** — فيُعيد المحاولة عبثاً على النماذج الستة (حتى ~18 نداءً مهدوراً
لكل دفعة)، والسبب الحقيقي يضيع (`error=None`) فلا يرى المستخدم *لماذا* تدهور كل شيء.

**الإصلاح:**
- [engines/ai_engine.py](engines/ai_engine.py): معالجة 403 صراحةً كحدّ على مستوى
  المفتاح (فشل سريع مثل 401) — قطع ~18 نداءً مهدوراً لكل دفعة إلى ≤6 (تحقّق: 1.2s).
- [services/ai_router_service.py](services/ai_router_service.py): دالة
  `_ai_failure_reason()` تسحب آخر خطأ حقيقي من `get_last_errors()` فتظهر للمستخدم
  «حدّ المفتاح مُستنفَد (403)» بدل «لم تُعاد استجابة» الغامضة (في مساري الفشل معاً).

**إجراء تشغيلي مطلوب منك:** استبدل/اشحن مفتاح OpenRouter — لا يُصلَح بالكود.

### 🟢 تحقّق: قانون حفظ الكتالوج يصمد في الكود (اللقطة المحفوظة قديمة فقط)

تدقيق بـ3 طرق: اللقطة الحيّة فيها فجوة `Σ(الأقسام)=11,038 ≠ الكتالوج=11,437` (399
مفقود)، لكن `recover_uncovered_catalog` على نفس البيانات يسترد **399 بالضبط** →
`Σ=11,437=الكتالوج، الفجوة=0`. أي **الكود سليم**؛ اللقطة المحفوظة سابقة لوصل
الإصلاح. **يكفي إعادة تشغيل التحليل لتحديث اللقطة.** (الدائرة المقفلة للمنافسين
متوازنة أصلاً: 129,647، 0% تسرّب.)

**تحقّق نهائي حيّ (تحليل كامل بالذكاء، المفتاح الجديد):** `Σ=11,437=الكتالوج، الفجوة=0` ·
الذكاء حسم 282 منتج مراجعة (165 ترقية + 117 استبعاد) بخطأ واحد · حارس التكلفة:
50 عيّنة من 19,744 (لا هدر) · الكاش يخفض التكرار من 34 دقيقة إلى **136 ثانية** ·
تنقّل حيّ (لوحة → سعر أعلى → جديد عند المنافسين): **صفر أخطاء console/خادم**، تصفيح
12 بطاقة/صفحة، بطاقات حيّة بشارات حرجية. 🟡 ملاحظة أداء: أول تحليل بلا كاش ~34 دقيقة.

### 🟡 جراحة: حذف 21 وحدة ميتة (engines/ + utils/)

**التشخيص:** خريطة وصول كاملة (AST) من `app.py` + `api/main.py` + `e2e_health_check.py`
كشفت 22 وحدة غير مُستدعاة من أي مسار تنفيذ. بعد تقاطع مع الاختبارات و`importlib`
والمراجع النصية، تبقّت **21 وحدة ميتة حقاً (صفر مراجع)** — كلها متعقَّبة في git
(قابلة للاسترداد)، استُثني `ui/components/product_timeline.py` (جديد قيد التنفيذ).

**المحذوف:** نسخ مُتجاوَزة من الكاشط (`ai_scraper_v27`, `scraper_v30_advanced`,
`concurrent_scraper`, `async_scraper`) ومحرّكات غير موصولة (`pricing_engine`,
`realtime_pipeline`, `reanalysis_engine`, `smart_router`, `analysis_job_runner`,
`scheduler`, `automation`, `anti_ban`, `challenge_engine`, `ai_fetch_router`,
`ai_selector_generator`, `file_reader`) وأدوات يتيمة (`product_analyzer`,
`db_adapter`, `filter_ui`, `health_check`, `jsonfast`).

**التحقق:** **692 اختبار ناجح / 0 فاشل** بعد الحذف · التطبيق يُقلع HTTP 200 ·
صفر أخطاء استيراد. كل الحذف قابل للاسترداد عبر `git checkout`.

## [سابقاً] — 2026-06-27

### 🔴 إصلاح حرج: ثقب حفظ بيانات الكتالوج (399 منتج يختفي بصمت)

**المشكلة:** بعد كل تحليل كان `Σ(الأقسام) = 11,038` بينما الكتالوج المرفوع
`11,437` منتجاً — أي **399 منتجاً من منتجاتنا يختفي بلا أثر** (لا مُسعَّر ولا
مُصنَّف ولا حتى مستبعد). السبب الجذري: محرّك التحليل يُرشّح التستر والأجهزة
والأطقم والمنتجات بلا مطابقة *قبل* إنتاج صفوف النتائج، فلا تظهر في أيّ قسم.
هذا خرق مباشر لقانون حفظ البيانات `total_in == Σ(الأقسام)`.

**الإصلاح:** دالة نقية جديدة `recover_uncovered_catalog` في
[services/classification_service.py](services/classification_service.py) تلتقط
كل اسم في الكتالوج غائب عن جميع الأقسام وتُعيد إدراجه في «⚪ مستبعد» بسبب واضح
(«بلا مطابقة سعرية — مُرشَّح تلقائياً»)، محاذى لأعمدة القسم. تعمل عند حدود خط
الأنابيب في [bootstrap.py](bootstrap.py) **بعد** تدقيق المحرّك (فيبقى تدقيق
المحرّك نظيفاً) فيُغلق توازن الكتالوج: `Σ(الأقسام) = 11,437 = عدد الكتالوج`.
لم يُلمس منطق المحرّك المحفوظ (`#PRESERVED_LOGIC`).

**الأثر:** صفر فقدان بيانات؛ المستخدم يرى الـ399 في «مستبعد» بدل اختفائها؛
العدد يظهر في إشعار اكتمال التحليل («♻️ N مُستردّ»). تحقّق على اللقطة الحيّة
(122MB) ومجرّد بـ 5 اختبارات وحدة نقية جديدة.

**ملفات:** `services/classification_service.py` · `bootstrap.py` · `app.py` ·
`tests/test_classification_service.py` · `tests/test_pricing_pipeline.py`

### ✅ التحقق
- حزمة الاختبارات: **692 ناجحة / 0 فاشلة** (+5 اختبارات نقية جديدة لشبكة الأمان).
- التحقق على البيانات الحيّة: `Σ(الأقسام)` ينتقل من 11,038 إلى 11,437 (= الكتالوج).
